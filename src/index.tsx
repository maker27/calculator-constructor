import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';

ReactDOM.render(
    <React.StrictMode>
        <div>App</div>
    </React.StrictMode>,
    document.getElementById('root')
);
